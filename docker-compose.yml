services:
  postgresql:
    image: postgres:15.2
    container_name: kalon-db
    environment:
      - POSTGRES_DB=kalon
      - POSTGRES_USER=kalon
      - POSTGRES_PASSWORD=kalon
    ports:
      - '5432:5432'
    networks:
      - kalon
networks:
  kalon:
    name: kalon
    driver: bridge
